# DeepLearningCode
The hybrid CNN-BiLSTM multi-model ensemble framework is implemented in MATLAB for the entire historical period (Scenario-1) and monsoon season (Scenario-2). The model performance is evaluated for the two scenarios. Input data for a sample grid point is provided in an Excel file, containing GCM data and IMD gridded rainfall data.
